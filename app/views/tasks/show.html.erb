<h1><%= @task.title %></h1>
<p><%= @task.details%></p>
<p><%= @task.completed%></p>


<a href="/tasks/<%= @task.id %>/edit">Edit</a>
<%= link_to "Delete",
            task_path(@task),
            method: :delete,
            data: { confirm: "Are you sure?" } %>
<a href="/tasks">Back</a>
